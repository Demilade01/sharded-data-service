apiVersion: v1
kind: ConfigMap
metadata:
  name: sharded-data-service-config
  labels:
    app: sharded-data-service
data:
  # Application configuration
  PORT: "3000"
  NODE_ENV: "production"
  SHARD_COUNT: "5"
  METRICS_ENABLED: "true"

